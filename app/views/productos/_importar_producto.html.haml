.importar_producto
    =form_tag "/empresas/#{@prefijo}/productos/import" , multipart: true do
        %table
            %tr
                %td
                    =file_field_tag :file
        %table
            %tr
                %td
                    Tipo GTIN
                %td
                    =select_tag "tipo_gtin", options_from_collection_for_select(TipoGtin.find(:all, :conditions => ["id in (?)", [1,3]]), "id", "tipo"), prompt: "- Seleccione -"
            %tr
                %td
                    =submit_tag "Importar"
                %td